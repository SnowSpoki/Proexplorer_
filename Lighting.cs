using UnityEngine;
using UnityEngine.EventSystems;	
using UnityEngine.UI;	public class LightingSwitcher : MonoBehaviour
	{
 {
    static double LightingSwitcher
    
    static void Main()
    {

        try
        {
            RenderSettings.skybox = skyBoxMaterial;
            lightingGameObject.SetActive(true);        }
        catch (lightingGameObject)
        {
            camera.SetActive(false);
        }
    }
}
